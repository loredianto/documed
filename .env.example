# Porte pubbliche: database e microservizi restano sulla rete Docker interna.
FRONTEND_PORT=3000
AUTH_GATEWAY_PORT=8282

# Chiave condivisa JWT. Impostare un valore locale non committato, minimo 32 caratteri.
JWT_SIGNING_KEY=replace-with-local-jwt-signing-key-at-least-32-characters
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
APP_TIME_ZONE=Europe/Rome

# PostgreSQL locale, condiviso tramite schemi separati da Patient e Auth Service.
POSTGRES_DB=documed_patient
POSTGRES_USER=documed
POSTGRES_PASSWORD=documed-local-password

# MongoDB locale, usato esclusivamente da Document Service e GridFS.
MONGO_USERNAME=platform
MONGO_PASSWORD=platform-local-password

# Auth: amministratori e client OAuth2 devono essere creati nello schema
# PostgreSQL auth_service prima del login. Non esistono account locali generati
# dal codice o dal compose.
FRONTEND_AUTH_CLIENT_ID=replace-with-db-oauth-client-id
FRONTEND_AUTH_CLIENT_SECRET=replace-with-db-oauth-client-secret

# OCR sincrono.
OCR_LANGUAGE=ita
OCR_TIMEOUT_SECONDS=60
