# Demo-only local values. Auth usa lo schema auth_service, Patient usa public.
# Replace them outside local development.
POSTGRES_PORT=5432
POSTGRES_DB=documed_patient
POSTGRES_USER=documed
POSTGRES_PASSWORD=documed-local-password
